apiVersion: v1
kind: Service
metadata:
  name: {{ include "bun-cloud-agent.fullname" . }}
  labels:
    {{- include "bun-cloud-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bun-cloud-agent.selectorLabels" . | nindent 4 }}
